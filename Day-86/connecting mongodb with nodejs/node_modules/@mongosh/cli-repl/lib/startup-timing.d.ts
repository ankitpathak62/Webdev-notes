import { type TimingCategory } from '@mongosh/types';
export declare function summariseTimingData(timingData: [string | TimingCategory, string, number][]): {
    [key: string]: number;
};
export declare const markTime: (category: TimingCategory, label: string) => void;
export declare const getTimingData: () => [string, string, number][];
